# 变更日志 · design-workflow

记录 design-workflow 技能的版本演进。技能正文（SKILL.md 与 modules/）只保留通用方法学，不内嵌署名。

## [子技能] design-mobile-app 1.0.4 → 1.0.5 - 2026-09-05

### 移除「八、注意力热力图」（用户判断无用）

- **用户指令**：「注意力热力图我感觉没用，删了」。整节移除：8.1 何时用 / 8.2 判分规则表 / 8.3 叠加层做法 / 8.3b 可运行实现（含三坑记录）/ 8.4 产出形态 / 8.5 边界。
- **同步清理**：description 触发词删「注意力热力图 / 视觉权重 / 热力自检 / 视觉显著性」；自检清单删对应「（可选）视觉权重自检」条目；父包 `SKILL.md` 路由表删「可选注意力热力图视觉权重自检」回写。
- **边界**：七节「写 APP 两步标准流程」不受影响。历史条目（1.0.3→1.0.4 新增、8.3b 补实现）保留作演进记录，不回溯改写。

## [子技能] design-mobile-app · 注意力热力图补可运行实现 - 2026-09-05
- **新增 8.3b「可运行实现（已验证做法）」**：第八节原本只定义方法与产出形态、无落地实现。补齐用 Python 脚本在既有原型 HTML 上注入 `.heatlayer` 的完整做法（正则提取 screen → 注入热斑 `<i class="heat hi|mid|lo">` → 开关按钮 + 随切屏更新的判分面板 → 坐标以手机画布为基准）。
- **记录三个必踩坑**：①Python 元组 `str()` 进 JS 会变成逗号表达式丢数据，须 `json.dumps(..., ensure_ascii=False)`；②注入的 `go()` 会覆盖原稿同名函数，原稿在 `go()` 内的状态切换（如记录态隐藏 FAB）须一并抄回；③改稿后布局变了必须同步改 `BLOBS` 坐标，否则热斑失位、自检结论失真。
- **实践来源**：trailmark-v3 徒步 App 原型做注意力热力图自检，检出 P1×2 / P2×2 / P3×2 共 6 处视觉权重偏差（记录态双 CTA 抢戏、照片过亮抢焦点、足迹四图等权无焦点、头像抢戏、路线库无推荐引导、海拔剖面与数据条同权），已按判读修复并重跑热力图验证。

## [第三轮整理] IP 解耦 + 结构图谱同步 · 2026-09-05
- **design-ip-assets 去硬编码（P2-5 收口）**：`subskills/design-ip-assets/SKILL.md` 正文里的「三果团」具体 IP 全部抽离为占位符/资产文件引用（SOP 步骤、生图 prompt 模板不再内嵌 三果团 配色/角色/符号数值），技能正文回归通用 schema；具体「三果团」示例保留在 `assets/` 下 5 个文件，并统一标注 `status: example` + 「示例」说明，由 `assets/_README.md` 明确"示例非强制资产、实际使用时替换"。
- **技能结构图谱同步**：`技能结构图谱.html` 子技能矩阵由 8 个扩到 **12 个**（补齐 design-metaphor-narrative / design-mobile-app / design-motion-vocabulary / design-prototype-divergence），来源署名计数 8→**11 份**，旧告警横幅改为"与 subskills/ 同步"说明。
- **删除**：`审美练习回路.html`（每日审美练习工具，用户要求移除），并同步移除 `SKILL.md` 目录树中的该行引用。根目录仅留 `技能结构图谱.html` 一个维护用 HTML。

## [第二轮体检] 一致性回潮修复 · 2026-09-04

### 磁盘审计后修复（读盘不靠记忆）

- **P0-1**：`subskills/design-taste/SKILL.md` 引言仍写"图片风格 + 动效 + 色彩 + 排版组合"，与三段式 schema（🟦/🟥/🚫）及"排版/交互/颜色 三维"矛盾 → 改为"用户显式定义的 排版 / 交互 / 颜色 三维，参考图事实与品味档位分离"。
- **P0-2**：主 `SKILL.md` 模式 2 小节仍写"含参考图 + 动效 + 色彩 + 排版组合" → 改为"用户定义的 排版 / 交互 / 颜色 三维；参考图事实与品味档位分离"。
- **P1-1**：主 `SKILL.md` 关键约定写场景层"4 个小节"，与目录树"五小节"及实际文件不符 → 改为"5 个小节"。
- **P1-2**：`design-taste` 选取规则/约束权重残留旧 7 字段词（色彩/形状/质感/排版/动效、形状/质感/排版构图）→ 改为"用户所发维度（🟥）一致/贯彻"。
- **P1-3**：`design-taste` 仍引用已废弃的 `accent` / `forbidden` 字段（新三段式用 🚫 红线替代）→ 改为"强调色纪律（颜色维度）"/"🚫 红线"。
- **P1-4**：`references/来源署名/README.md` 来源清单漏列实际存在的 `icons8.md`（共 11 份只列 10）→ 补 Icons8 行。
- **P1-5**：`技能结构图谱.html` 硬编码"8 个专项子技能"且 SKS 数组仅 8 张卡（实际 12 子技能 / 12 源）→ 顶部加"静态快照可能过时，以 SKILL.md 为准"醒目声明，并去掉错误计数。
- **P2-2**：主 `SKILL.md` 目录树漏列实际存在的 `references/image_gen_prompt_templates.md` → 补入 references 树。
- **P2-3**：`design-taste` metadata 段注明 `owner_note` 等为非约束注释字段。
- **P2-4**：`design-taste` description 三段式顺序（🟥/🟦/🚫）与正文/实例（🟦/🟥/🚫）不一致 → 统一为 🟦/🟥/🚫。
- **P2-6**：删除两个空残留目录 `.tmp-preview/`、`generated-images/`。
- **P2-5（已于 2026-09-05 收口）**：`design-ip-assets` 原内嵌的「三果团」具体 IP 已抽离——`SKILL.md` 正文回归通用 schema（占位符/引用 `assets/*.md`），具体「三果团」示例保留在 `assets/` 下 5 个文件并标注 `status: example`，由 `assets/_README.md` 明确"示例非强制资产、实际使用时替换"。P2-1 根目录两个 HTML 已登记在树中、属有意保留的维护工具（其中 `审美练习回路.html` 已于 2026-09-05 用户指令删除，仅留 `技能结构图谱.html`）。
- **版本**：`design-taste` 1.0.2 → 1.0.3。

## [子技能] design-taste 1.0.1 → 1.0.2 - 2026-09-04

### 容器 schema 强制三段式落地（P0-1 · 方案 A）

- **用户决策**：P0-1 走方案 A —— schema **强制三段式**，把"档位不是模板"原则焊进容器，防止实例漂回写死字段。
- **改动（design-taste SKILL.md 1.0.1 → 1.0.2）**：
  - 重写「一、Profile Schema」：废弃原写死 7 字段（colors/shapes/texture/typography/motion/references/forbidden），改为**强制三段式** = 🟦 参考图事实 / 🟥 品味维度（仅用户原话，AI 不增补）/ 🚫 红线；metadata 仅留 id/name/summary/references/composable_with。
  - 新增「标准模板」markdown 骨架，供新增实例直接套用。
  - 第五层终检「字段完整」改为「三段式完整」，并去除对旧 7 字段（色彩温度/圆角/质感）的隐含依赖。
  - description 增补「实例档案强制三段式」；version bump 至 1.0.2。
  - 同步 `taste-profiles/_README.md` 新增步骤：正文必须三段式。
  - 同步既有实例 `athens-week.md`：补「🚫 三、红线」块（原红线散落各段，统一收口），原「三、交付校准」顺延为「四」。

## [主技能] 恢复「各层交叉资产」段落 · 2026-09-04

### 用户确认需要方法论→子技能映射（复用资产地图）

- **背景**：上一条「清理与边界强化」把「### 各层交叉资产」整段移除；用户随后澄清——"交叉资产"指的是**用户私有的模式 2 参考源**（athens-week 的参考图/链接），不应外泄或当通用资产；而"哪些外部方法论被吸收进哪些子技能/子位置"这份**方法论→子技能映射**是需要的复用资产地图，必须保留。
- **改动**：将「### 各层交叉资产（已并入子位置文件）」整段（22 行）原样恢复至 SKILL.md 第 5 层步骤之后、「## 怎么用这个工作流」之前（位置与原文一致）。
- **边界仍生效**：模式 1/模式 2 互不干扰、用户参考源私有（见 `subskills/design-taste/SKILL.md` 第六节）的约束不变；本次仅恢复通用方法论归属地图，不涉及任何用户私有素材。

## [主技能] 清理与边界强化 · 2026-09-04

### 移除「各层交叉资产」段落 + 删除 灵感App.html + 模式2品味吸收规则

- **移除交叉资产列示**：删除 SKILL.md 中「### 各层交叉资产（已并入子位置文件）」整段（原 23 行）。该段逐条列示各专项方法论被吸收进哪些子技能/子位置，属跨层交叉资产说明；移除后主 SKILL.md 只保留通用流程索引，各方法论来源署名仍存于 `references/来源署名/`。
- **删除 demo 产物**：删除 `灵感App.html`（lumina 演示页，用户明确要求清除）。
- **新增模式 2 品味吸收规则**（`subskills/design-taste/SKILL.md` 第六节）：用户给新的链接/图片作为模式 2 品味来源时，**必须**经 design-workflow 五层「骨架」拆解 → 沉淀为「🟦 参考图事实」→ 提炼「🟥 用户维度」→ 留存 `taste-profiles/<id>.md`；并明确**模式 1/模式 2 互不干扰**，且用户的参考源为私有素材、不得当作通用资产对外提供。
- **后续（已解决）**：P0-1 已于同日走方案 A 落地，见 `[子技能] design-taste 1.0.1 → 1.0.2`。

## [子技能] design-taste 1.0.0 → 1.0.1 - 2026-09-04

### 描述与正文矛盾修复 + 版本同步（技能体检 P0-2）

- **来源**：2026-09-04 技能体检（用户要求不靠记忆评价 design-workflow，仅读盘审计）。发现 design-taste frontmatter `description` 与正文规则 5（维度只收用户原话、AI 不增补）矛盾——description 仍写「含参考图 + 动效 + 色彩 + 排版组合」，会把下轮模式 2 的实例选取方向带歪。
- **改动（design-taste SKILL.md 1.0.0 → 1.0.1）**：description 重写为「实例只收录用户显式定义的维度原话（当前 athens-week：排版/交互/颜色 三维）；参考图内容事实与品味档位分离；品味是档位非模板」；version bump 至 1.0.1。同步修 `taste-profiles/_README.md` 过期 NOTE（原称"参考图占位待补"，实际 `athens-week-fullpage.png` 已在目录且档案铁律 4.1 要求必读原图）。
- **决策背景**：容器 schema（7 字段）与实际实例结构（3 维 + 🟦事实分离）的分叉（P0-1）；该分叉已于同日走方案 A 落地为三段式，见 `[子技能] design-taste 1.0.1 → 1.0.2`。

## [子技能] design-mobile-app 1.0.3 → 1.0.4 - 2026-09-03

### 注意力热力图（可选 · 视觉权重自检 · 方案 A）

- **来源**：用户提出「应用设计方面增加注意力热力图」；经对齐，定位为**可选视觉权重自检**（非强制闸门），数据来源走**方案 A（设计原则判定）**，不接显著性模型（对 UI 截图失真 + 沙箱受限）。
- **改动（design-mobile-app SKILL.md 1.0.3 → 1.0.4）**：新增「八、注意力热力图」——8.1 何时用（UI 稿/运行截图后校验，可跳过）；8.2 视觉权重判分规则表（对比度/字号字重/位置/颜色/动态/内容类型/留白/扫视路径 8 维度，高权重红区 vs 低权重蓝区）；8.3 生成叠加层做法（逐元素打分 → Canvas/SVG 径向渐变热斑 红→黄→蓝 半透明叠回 → 理想vs实际对照判读 → 改稿前后对比）；8.4 产出形态（单页 HTML 原图+热力层对照，可并入全流程巡查页）；8.5 边界（不接显著性模型、非用户研究数据）。触发词补「注意力热力图/视觉权重/热力自检/视觉显著性」；自检清单补一条（可选）。
- **决策背景**：用户曾追问「到底要不要加、为什么加」；结论——它补的是「视觉注意力分配」层（和 C 段三动作里眯眼查焦点互补、可量化前后对比），不解决「跳过线框」根因（那已被两步流程解决），故定位为提质补充而非救火必需。

## [子技能] design-mobile-app 1.0.2 → 1.0.3 - 2026-09-03

### 写 APP 两步标准流程（线框图起步 · 全流程巡查贯穿）

- **来源**：用户抖音截图提示词（vibecoding 实战），已逐字提取。
- **用户定调**：「第一个是要开始做的，默认；第二个是流程中要做到全流程」——线框图是写 APP 的默认起步动作，全流程巡查是开发中必做、覆盖正常 + 分支 + 异常状态。
- **改动（design-mobile-app SKILL.md 1.0.2 → 1.0.3）**：新增「七、写 APP 的两步标准流程」——7.1 低保真线框图（默认起步，附可直接套用提示词：PRD→单页 HTML 线框，平铺全部核心页面 + 关键状态 + 待确认）；7.2 页面全流程巡查（开发中贯穿，附提示词：本地走查页带「可以 / 待改 / 阻塞」标记、反馈落本地、导出 Markdown / JSON）；开头加「调用约定」明确两步为默认动作；触发词补「线框图 / 低保真 / PRD 走查 / 全流程巡查 / 页面巡查 / 写 APP」；自检清单补一条标准两步。
- **落点说明**：用户确认落点为 `design-mobile-app`（移动端技能），不碰 `autonomous-engineering-factory` 核心文件（遵守既有规矩）。

## [子技能] design-reference-sourcing 1.2.1 → 1.2.2 - 2026-09-03

### 用户新增 3 个参考源（其中 Unicorn Studio 已存在）

- **用户指令**：把 `https://styles.refero.design/`、`https://www.unicorn.studio/inspiration`、`https://www.awwwards.com/websites/sites_of_the_day/` 三个加入参考源。
- **实际落点**：
  - **Unicorn Studio（Inspiration）**：经核已存在于开放源清单（1.2.0 起），无需重复添加，仅确认保留。
  - **Refero Styles**（`https://styles.refero.design/`）：新增。实测为「DESIGN.md 示例库（面向 AI 设计 Agent 的规格写法范例）」，**非** UI 截图检索主站；补进开放源清单 + 「按键选源」设计规格写法路由 + 开放源列表（总数 8 → 10）。✅ 用户确认：此址正是其想要的参考源——价值在于"可直接抓取真实 DESIGN.md 范例来读设计规范 / 规格约定"，赋能本技能产出 DESIGN.md / 机器可读设计规格；**明确不需要** UI 截图检索主站 `refero.design`（不添加）。
  - **Awwwards（Sites of the Day）**（`https://www.awwwards.com/websites/sites_of_the_day/`）：新增。网页创意/交互前沿成品鉴赏，补进开放源清单 + 「按键选源」网页动效路由 + 开放源列表。
- **联动更新**：策展源清单计数 10 → 12（开放源 8 → 10，登录源 2 不变）；description 与「优先用 N 个开放源」「一·补充 按键选源」同步；父包 SKILL.md 第 121 行引用计数同步为 12；version 1.2.1 → 1.2.2。
- **抓取手法**：Refero 落地页可读、WebFetch 直取筛选维度；Awwwards feed 为 JS 渲染、用 agent-browser 截图读视觉（见该子技能第七节）。

## [子技能] design-mobile-app 1.0.1 → 1.0.2 - 2026-09-02

### 补工程规范基线（设计稿标注层，不约束审美）

- **来源**：用户提供的移动端 UI 设计规范教程（设绘玩家UED 微信公众号）提炼。
- **用户定调（方案 A）**：只取该教程的"工程规范"部分，**不取"风格取向"**——教程中"字号偶数等差、层级差 2–4px"与用户定的"极端跳档、拒绝等差"冲突，已明确排除。
- **改动（design-mobile-app SKILL.md 1.0.1 → 1.0.2）**：新增「工程规范基线」小节，含尺寸基准 750×1334(@2x)/@3x/@1x 适配、系统组件层预留、边距下限 20px / 卡片间距 16–40px、按钮三态、提示框模态/非模态、图片比例；并加醒目标注"工程约束，不约束审美，冲突时审美优先、不含字号层级规则"。自检清单补一条工程基线自检。
- **加进位置的边界**：未进参考站策展源（教程非案例库）；未进 taste-profiles（与 athens-week 品味冲突）。仅补工程严谨度，治"设计稿漂亮、开发还原差"的断层。

## [1.6.2] - 2026-09-03

### 父包索引同步维护（修上一轮评价发现的失同步）

- **动因**：上一轮带分评价（8.5/10）实指「父包索引失同步」维护风险已兑现——每次子技能演进后父包需手动同步多处，已漏改。本次集中回写。
- **改动（SKILL.md 主控 1.6.1 → 1.6.2）**：
  - 目录树（原第 167 行）：`design-reference-sourcing` 源数「开放 8 + 登录 2 = 10 源」→「开放 10 + 登录 2 = 12 源」（子技能此前已增到 12 源，父包描述第 121 行改了，目录树漏改）。
  - 专项子技能路由表（原第 191 行）：`design-mobile-app` 路由描述补回写「写 APP 两步标准流程（线框图默认起步 + 全流程巡查贯穿）+ 可选注意力热力图」，此前子技能已演进但父包路由未回写。
  - 清理工程残留：删除 `skill-optimizer` 跑批快照 `.codebuddy/`（含已删 taste profile 旧 runs）与 `references/__pycache__/*.pyc` 字节码缓存，符合本地测试隔离约定。

## [1.6.1] - 2026-09-01

### 厘清模式分工：品味基线专属模式 2，模式 1 不调用（修正 1.6.0 的越界兜底）

- **用户定调**："品味基线是给我第二个用的，第二个模式用的，这才是要符合我的审美的。第一个就是要找参考，做常规设计。"
- **问题**：1.6.0 把模式 1「参考检索」的兜底写成了"用品味基线兜底 / UNREFERENCED + 品味基线"，等于把模式 2 的 `design-taste` / `taste-profiles` 漏进了模式 1，与定调冲突。
- **改动（SKILL.md 主控 1.6.0 → 1.6.1）**：
  - 模式 1 小节「参考检索」：去掉"（与品味基线并列）"措辞；兜底改"按常规设计默认值推进（**不调用模式 2 的 taste-profiles 品味基线**）"。
  - 「怎么用」第 1.5 步兜底同步修正。
  - 模式 2 小节新增「**品味基线专属本模式**」：design-taste / taste-profiles 仅在本模式激活，模式 1 绝不调用。
- **改动（design-reference-sourcing 1.2.0 → 1.2.1）**：
  - 「一·补充」第 5 步去掉"（与品味基线并列）"；第 6 步兜底改"按常规设计默认值推进（沿用通用反 slop 护栏，但不调用模式 2 的 taste-profiles 品味基线）"。
- **最终分工（写死）**：模式 1 = 参考站做常规设计（检索不到→常规默认，不碰 taste-profiles）；模式 2 = 品味基线（design-taste / taste-profiles）即用户审美；原修复建议③（模式 1 接品味基线）**作废**，不再实施。

## [1.6.0] - 2026-09-01

### 模式 1 默认强制参考检索（用户定调：输入 brief 就自动去参考站找对应的设计）

- **用户定调**："我希望是我的第一个默认用参考的……我要输入 brief 的时候，它要自己去那里找对应的设计来给我做"——参考站从「用户喊才醒」（按需取用）升级为「模式 1 默认路径强制前置」（每次启动必查）。
- **改动**：
  - `design-reference-sourcing` 1.1.1 → 1.2.0：description 与「一、何时触发」把「模式 1 默认路径·强制」列为首要用途；新增「一·补充 · 模式 1 强制检索流程」六步（提取检索键 → 按键选源 → 省流抓取 2–4 个 → 红点笔记提炼 → 注入 L2 → 检索不到兜底 `UNREFERENCED` + 品味基线，不阻塞流程）；计数 9 → 10 源。
  - SKILL.md 主控：模式 1 小节新增「参考检索（本模式强制前置）」；「怎么用」新增第 1.5 步（仅模式 1 套用）；路由表与目录结构同步；version 1.5.9 → 1.6.0。
- 边界：模式 2 仍走 design-taste 参考图、模式 3 自由探索，不套用本强制步；品味基线（修复建议③）仍挂起。

## [1.5.9] - 2026-09-01

### 场景层新增「交付形态」判定（修复建议②落地 · 本技能只做设计）

- **用户定调**："2.只做设计呀"——design-workflow 是设计技能，交付物是设计稿 / 原型，不扩到"可运行真应用（Expo / 部署）"那条工程线。
- **改动**：
  - `modules/01-场景.md` 载体小节新增 **4.6 交付形态（设计产物判定）**：① 边界红线——本技能只交付设计物（静态图/海报、网页界面稿、移动端界面稿、叙事长页、视频/动效分镜、品牌规范），可运行应用 / 可部署系统归**工程线（ae-factory）**；② 判定四步（问形态 → 落格式 → 钉验收 → 防串味）；③ 交付形态表（每形态对应第 5 层验收标准）。
  - SKILL.md 第 1 步「载体」描述补交付形态引用；version 1.5.8 → 1.5.9。
- 说明：修复建议③（模式 1 接品味基线）、④（冻结扩张）仍挂起，等用户指示。

## [1.5.8] - 2026-09-01

### C 段三动作设为模式 1 交付前硬闸门（修复「好看」闸门被外包给用户）

- **根因**：`references/审美精度标准.md` C 段三动作（缩略图 25% / 眯眼 8px 模糊 / 倒转）已明确"全过才算过好看关"，但主控 SKILL.md 第 7 步写着「默认不自动核查：先给人看成品」，把「好不好看」这道闸门外包给了用户 → 四次交付反复被否（我做→你否→我改→你否）。
- **改动**：
  - 模式 1 小节新增「交付硬闸门（本模式强制）」：完稿后、给人看之前必须先跑 C 段三动作，全过才交付；没过打回第 2–3 层修正，不许带着形式缺陷给人看。
  - 「怎么用这个工作流」第 7 步：由「默认不自动核查：先给人看成品」改为「交付前硬闸门（模式 1 强制）」，三动作全过才允许交付；技术校验仍按需、在形式过关之后。
  - 「技能工程约定」：由「先人审后机检」改为「先形式自检后交付（模式 1 硬闸门）」。
- 说明：三动作是纯视觉检查，对着成品图 5 秒跑完、不需要浏览器；只改主控接线，不动 `references/审美精度标准.md` 内容本身。version 1.5.7 → 1.5.8。

## [1.5.7] - 2026-09-01

### 新增 2 个设计向子技能（emilkowalski 动效体系吸收，设计侧落地）

- `subskills/design-prototype-divergence/`（v1.0.0）：原型发散——同一 UI 件建 3–5 个**真正不同**的版本 + 可视化选择器，探索/模式 2 吸睛用。来源 emilkowalski `prototype`。
- `subskills/design-motion-vocabulary/`（v1.0.0）：动效术语反查词典——模糊描述→精确术语，沟通层。来源 emilkowalski `animation-vocabulary`。
- 目录树 + 路由表各加一行。
- 说明：emilkowalski 其余 6 个工程向技能（motion-foundations / motion-recipes / motion-review / motion-opportunities / apple-fluid-motion / ui-library-selector）按用户决策进 ae-factory（经 `ae-factory-experience-overlay` 非侵入路由），不进 design-workflow。

## [1.5.6] - 2026-09-01

### design-reference-sourcing 1.1.1 → 1.1.2（新增 b2tf.app 移动端参考）

- 「二·补充 · 按资源类型分类的设计参考站」表新增 **b2tf.app**（Vintage Mobile App Design Directory）：复古风移动 App 设计目录，按分类策展真实 App 截图，免登录可直抓。
- 定位：与 mobbin.com 互补——mobbin 偏"真实产品通用 UI 模式"，b2tf 偏"vintage 美学在移动端的落地"，属移动端审美参考而非工具站。
- 表头「精选 4 个 → 5 个」；子技能版本 1.1.1 → 1.1.2。

## [1.5.5] - 2026-09-01

### design-mobile-app 1.0.0 → 1.0.1（柔化"结构优先"为"按指令改皮肤/骨架"）

- **用户校准**：此前把"宠物 APP 只换皮肤"教训推导成刚性规则「改 UI 先动结构，皮肤最后动」是过度推导。用户明确：不是一定要换骨架；正确顺序是**先换皮肤、后换骨架**，且换骨架只在用户要求时、或皮肤结构一起换；核心是「UI 设计」本身，跟着用户方向走，不自行强加结构流程。
- 改动 `subskills/design-mobile-app/SKILL.md`：① description 去掉"结构优先"措辞，改为"结构手法 + 按用户指令改皮肤或骨架"；② 一节标题「先动结构，皮肤最后动（根因）」→「结构手法（按用户指令使用，非强制首步）」，开头明确"顺序以用户指令为准，默认可先调皮肤，结构按用户要求再动或一起动，不强行每轮改骨架"；③ 一节自检 / 接驳段 / 自检清单首条均加"仅在用户指向结构问题时才算缺陷"的限定；④ version 1.0.0 → 1.0.1。
- 同步：`~/.workbuddy/MEMORY.md` 的「UI 设计感在结构层」条目已改写该原则并加 2026-09-01 校准说明（结构手法是工具箱，不是强制首步）。

## [1.5.4] - 2026-09-01

### 新增子技能 design-mobile-app（移动端 App 界面/原型专项）

- 针对用户痛点"移动端 App 设计像 AI slop（只换皮肤不换骨架、卡片等距堆叠）"，新建 `subskills/design-mobile-app/SKILL.md`（v1.0.0）。
- 核心：把"结构优先、反皮肤堆叠"原则落到移动范式——6 条结构手法（hairline 分区/密度节奏/字号跳档/网格对齐/不对称/字重对比）+ 移动专属硬约束（拇指热区/safe area/一屏一焦/触摸目标≥44pt）+ 原生组件语汇（tab/sheet/FAB/segmented/list row/skeleton/手势）+ 生图织进结构。
- 与 `design-frontend` 明确分工：frontend 管通用交互/UX 写作/动效意图，本技能管移动专属结构约束与原生范式，二者叠加。
- 父包 SKILL.md 目录树与路由表均登记；本文件 frontmatter 1.5.2 → 1.5.4（补记此前遗漏的 1.5.3 Codrops 条目）。

## [1.5.3] - 2026-09-01

### design-reference-sourcing 1.1.0 → 1.1.1（新增 Codrops Creative Hub 参考源）

- 开放策展源清单新增 **Codrops Creative Hub**（https://tympanus.net/codrops/hub/）：WebGL / Three.js / GSAP / SVG 的 demos、tutorials、sketches 与 UI 模式聚合站，免登录；与 ThreeUI / React Bits 互补，补全「前端动效实现参考 + 交互模式库」线。
- 同步更新技能描述、4 处内联源列表、「9 个 → 10 个」计数；子技能版本 1.1.0 → 1.1.1，并新建子技能级 CHANGELOG.md。

## [1.5.2] - 2026-09-01

### 双评审闸门·省流截图 SOP（把最慢环节压到几十秒）

针对 1.5.1 实战里"agent-browser 截图反复冷启动 + `close` 不干净导致无头实例堆积、越跑越慢（单次卡到分钟级）"的痛点，补进 `modules/05-主观输出/感觉.md` 双评审闸门「如何实跑」小节：

- **四步省流 SOP**：① 先静态自检（Read/Grep/Python 像素测量 `vmap` 思路）秒级拦错，能不开浏览器就不开；② 截图前先 `agent-browser --help` 锁命令、不边试边跑；③ 单次截图 + 关闭校验（`open→screenshot→close` 后立刻 `tasklist` 查无头实例是否真退，没退 `taskkill /F /PID` 补刀）；④ 基于一张图精修，再截严格 ≤2 轮。
- **进程清理安全红线**：清理残留 chrome **绝不用** `taskkill /F /IM chrome.exe`（误杀用户 Chrome）；只杀明确无头实例（命令行含 WorkBuddy `app-id` / `--headless`），若进程树混入带 `User Data` 的正常 Chrome 即停手、不 `taskkill /T`。
- 来源：2026-09-01 实测踩坑（反复 open/close 触发 agent-browser 守护进程自动重生，无头实例越积越多；差点误杀带 `User Data` 的正常 Chrome）。

- version 1.5.1 → 1.5.2。

## [1.5.1] - 2026-08-31

### 双评审闸门实战验证（三轮迭代跑通）+ 补「如何实跑」管线

**验证任务**：生一张怀表静物图 → 按 design-workflow 做「物件档案卡」海报 → 截图 → 合成并排比对 → 派两个独立子代理评审 → 迭代到通过。

| 轮次 | 复合分 | 盲 scout | 未过的关键特征 | 抓到的问题 |
|---|---|---|---|---|
| 1 | 0.52 | reject | K2 表冠被裁、K4 黄铜去饱和 | **signature 双环被 `overflow:hidden` 裁掉、实际完全不存在**（设计者自以为画了） |
| 2 | 0.63 | reject | K2 表冠仍被裁、K6 第二道环不可见 | 环移出容器后太细太淡（1px / 0.18 透明度） |
| 3 | **0.78** | **approve** | 全部通过 | — |

**补进技能的内容**（`05-主观输出/感觉.md`「双评审闸门」新增小节）：

- **工具链**：`agent-browser open file://<绝对路径>` + `screenshot --full` 可截本地 HTML（**不必起本地服务**）；并排比对图用 Python + PIL 合成（等高对齐 + 中性标签，只标身份不标意图）。
- **prompt 要点**：盲 scout 必须明令"不会被告知这是什么、brief 是什么、改了什么，不要猜测意图"，禁打分禁给参数，并加"看不到图就输出 `CANNOT_PERCEIVE_IMAGES` 并停止，不要编造"的兜底；主审给完整 spec + 关键特征清单 + 可编辑 ID + 迭代史，**扣下设计者自提的分数**；两者互不告知结论、并发派发。
- **已验证经验**：① 子代理确实能看图，产出具体到区域与方向；② 闸门能抓到设计者完全没发现的硬 bug（signature 元素被裁掉而不自知）；③ **评审结果本身也要核实**——本轮 scout 报"SOURCE 水印残留到 CURRENT"，程序验证为**半对半错**：水印在 SOURCE 属实（亮像素 5.06% vs 对照 0.00%），但 CURRENT 为 0.00%，**并未残留**；scout 会误报"是否残留"这类跨图推断，落地前应用程序核一遍；④ 判据要看 K 特征逐个过闸，不是只看总分——三轮提升全靠明确的 K 失败项驱动。

- version 1.5.0 → 1.5.1。

## [1.5.0] - 2026-08-31

### 吸收 vinhhien112/img2obj（MIT）：双评审闸门 + 冠军/挑战者 + 四阶段建模执行流程

**来源**：https://github.com/vinhhien112/img2obj —— 参考图 → 纯代码程序化 Three.js 资产的质量闸门工作流（Codex 插件）。用户确认按「A 抽评审机制 + B 补模型执行流程」两路吸收。

**A · 通用评审机制（任何有目标稿的视觉产出都适用）**

- `modules/05-主观输出/感觉.md` 新增「**补充 · 视觉验收的双评审闸门（builder 不能给自己打分）**」：
  - **盲视觉 scout**（独立子代理）：只拿目标稿 / 当前 / 上一版 / 并排比对图；**禁止**拿意图卡、spec、ID、参数、分数与 builder 辩护；先扫早期缺陷再审当前；最多 7 条方向，不打分数不改东西；被禁内容泄漏即报 `UNVERIFIED`。
  - **主独立评审**（独立子代理）：复合相似度分 + **逐个关键特征过闸** + 精确数值修正（对象/差异/期望/操作/参数路径/可证伪证据）。
  - **关键特征否决制**：放行需同时满足「复合分达标 + 每个关键特征过闸 + scout 放行」，**总分高不能掩盖关键特征失败**。已给出平面设计的对应物：logo 轮廓 / 主视觉主体 / 标题层级 / 品牌色落点 / signature 元素。
  - **冠军 / 挑战者**：修改只作用于挑战者、失败事务性回滚、连续三次无实质改进 = 策略耗尽须换表示法。
  - **编辑前影响评估**：目标 / 受保护对象 / 结构不变量 / 副作用 / 下游影响（含缓解与将来验证）/ 回滚点。
  - **四条明确禁止**：量化分数不得替代并排视觉比较；未观察到的结构不得写成事实；不得跨阶段提前施工；检查跑不了须报 `UNVERIFIED`。
- `感觉.md` 自检清单 +2 条；`subskills/design-frontend` 自检清单 +2 条（条件项：有目标稿/设计稿/参考稿时）。

**B · 模型模块补执行流程**

- `modules/02-感知物料/模型.md` 新增第 5 节「**从参考图到模型的执行流程**」（原第 5 节顺延为第 6 节）：
  - 四阶段表：Blockout 轮廓 / Form 结构 / Lookdev 材质 / Interaction 可动（含各阶段明确不该做的事）。
  - 渐进式规格 = 稳定核心 + 阶段增量；首次渲染前不写满最终规格。
  - 单循环：影响评估 → 只改挑战者 → 构建/渲染 → 与唯一权威参考图并排比对 → 双评审 → 晋级/回滚 → 人工批准。
  - 门禁要点：**唯一权威参考图**（合成/辅助视图只能否证不能证明）、证据范围错配无效、冠军/挑战者、三连无改进换表示法、未观察部分记「有界假设」。
  - 能力分支按需加载（禁止预载、禁止整体归一类）；明确不适合清单（敢于说不）。
- `模型.md` 自检清单 +4 条。

**合规与边界**

- 新建 `references/来源署名/vinhhien112-img2obj.md`（MIT，逐源单文件），已登记进来源署名 README 索引；来源计数 10 → 11 份。
- **未吸收（3D 领域执行物）**：Three.js/TypeScript factory、`sculpt.py` CLI、`ObjectSculptSpec` schema、PBR 通道工程、拓扑与 pivot/socket 机制、capability packs 领域内容、Codex 插件绑定。
- **边界已写死**：四阶段与 PBR 概念**仅限任务确实要照图做模型时触发**；是否上 3D 仍由 `模型.md` 第 1~4 节选型决策判定。吸收的是「评审与迭代机制」，不是「3D 本身」——与用户此前"强行上 3D 反而不如干净 2D"的负面经验对齐。
- version 1.4.3 → 1.5.0。

## [1.4.3] - 2026-08-31

### 参考站精简（用户校准：工具/推广站不算"设计参考"）
- 用户指出 1.4.2 添加的 10 站"都不算什么参考"——校准"设计参考"定义：**只收能学到审美规律的作品 / 灵感源；素材 / 工具 / 模板 / 推广站不收**。
- `design-reference-sourcing`「二·补充」从 10 站精简为 **4 站**：curations.supply（策展库）/ 60fps.design（动效参考）/ mobbin.com（移动模式库）/ rebrand.gallery（品牌案例）；删除 landing.love / saaspo / sleek.design / uncut.wtf / hugeicons 及待补的 Design Systems。
- 段内已写死筛选标准，防止以后再混入工具站。
- version 1.4.2 → 1.4.3。

## [1.4.2] - 2026-08-31

### 设计参考站 URL 库（按资源类型分类）
- `subskills/design-reference-sourcing` 新增「二·补充 · 按资源类型分类的设计参考站」小节：10 个常用设计资源站按 9 类组织（设计库 / 落地页 / SaaS / AI App Builder / 字体 / 动画 / 移动 App / 品牌 / 图标 / 设计系统待补）。
- 与「二、策展源清单」互补——策展源按平台/抓取方法（批量入 taste-profiles），本表按资源类型（日常找灵感速查）。
- URL 已去掉发布者的 `?ref=` / `?via=` 推荐识别码（保持链接干净，与功能无关）。
- 来源：用户提供截图书单（个人收藏，非外部方法学吸收）。
- version 1.4.1 → 1.4.2。

## [1.4.1] - 2026-08-31

### 动态手法 + 手感规律（提炼自 curved gallery 学习）
- `references/惊艳手法库.md` 新增 2 个**动态类**手法（与既有 8 个静态手法并列）：
  - 手法 9「磁吸倾斜」：卡片 / 主体跟指针轻微转向 + 悬停圆顶凹陷（clamp 0.08–0.92 / tilt ≤0.1 rad / 指数平滑追随）。
  - 手法 10「速度驱动色差」：运动越快 RGB 分离色差越强（强度 ∝ 速度，静止归零）。
  - 均为通用动态技法归纳（磁吸倾斜 / 色差是广泛使用的交互与后处理手法，非特定实现归属），延续「思想层不设限」边界，不建来源署名文件。
- `02-感知物料/动画.md` 新增「指数平滑追随手感」：`1 − exp(−k·dt)` 帧率无关跟手公式（含 k 参数表与用途），与缓动速查表互补（曲线表管一次运动，本公式管持续追随）。
- 学习来源：sun090909090/dongxiao 的 curved gallery（Three.js + GLSL 实现）——仅提炼通用手法与参数规律，未吸收其实现代码。
- version 1.4.0 → 1.4.1。

## [1.4.0] - 2026-08-31

### 交互工程对错清单（吸收 emilkowalski/skills · MIT）
- `design-frontend` 新增「八、交互工程对错清单（功能型产品 UI / APP 专用）」：12+ 条对错（transition:all / scale(0) / ease-in 入场 / transform-origin 锚定 / 键盘操作不动效 / hover 媒体查询门控 / 高频用 transition 不用 keyframes / Enter-Exit 不对称）+ 参数表（三根自定义曲线 / 时长表 ≤300ms / spring / scale 0.97 / stagger 30-80ms / blur ≤20px）+ Review `Before|After|Why` 三列格式。
- **适用边界（用户指定）**：仅功能型产品界面 / APP 交互组件（写程序 / 做 APP）；**不适用**设计感网页 / 海报 / 品牌页 / 叙事长页（那些走 `动画.md` 数量预算+破格 与 `惊艳手法库`）。动画.md 已加边界指针，防止误用。
- 来源署名：新增 `references/来源署名/emilkowalski-skills.md`（MIT）；README 索引登记；来源计数 9 → 10 份。
- version 1.3.1 → 1.4.0。

## [1.3.1] - 2026-08-29

### 审美精度层（补齐「不出错」到「好看」之间的断层）
- **新增 `references/审美精度标准.md`**（三段）。诊断：既有资产里意图卡给**方向词**、惊艳手法库给**破格点**、反模板防 **AI 味**、技术校验防**硬伤**——但「不出错」到「好看」之间缺一层**精度**。一个设计可以方向对、无硬伤、不 AI 味，却依然平庸；平庸通常不是方向错，是**数值糊**。
  - **A 段 感觉词 → 参数映射表**：5 个感觉词族（克制精密 / 高雅编辑 / 戏剧冲击 / 温暖手作 / 活泼）× 5 个可测参数（字号阶梯比 / 灰度层级 / 间距倍率 / 字重对比 / 细节密度）；硬要求每词翻译成 ≥2 个参数；「盖住词只看参数能否反推感觉」的反向校验。
  - **B 段 精度标准 8 条**：全是数值不是原则。含最易暴露业余的「**大字号反向收紧字距**」（≥48px → −0.02~−0.04em，绝不给大标题加正字距）、「间距等比非等差」、「灰度 ≥4 级」、「圆角/线宽/阴影同源」、「对齐挂网 ≥80%」、「放大 200% 近看有东西」。
  - **C 段 三个物理验收动作**：缩略图 25% 看三级 / 眯眼（8px 模糊）看剩 3–5 个视觉重量块 / 倒转看纯形式平衡。**明确先于技术校验跑**——先形式后硬伤，否则会陷入"一直在修溢出、从没看过整体"。
- **接线四处**：`01-场景.md` 审美意图卡（A 段 + 自检项 + 「方向→精度→记忆点」说明）、`03-空间.md` 间距等比与对齐挂网、`03-排版构图.md` 新增「落位精度校准」补充段（B 段三条最常翻车项）、`05-感觉.md` 新增「好看验收：三个物理动作」（C 段，置于技术校验之前）。
- SKILL.md：交叉资产新增一条（含与既有资产的四段分工：方向 / 精度 / 记忆点 / 硬伤）、目录结构补文件。
- version 1.3.0 → 1.3.1。

## [1.3.0] - 2026-08-29

### 新增专项子技能 `design-metaphor-narrative`（隐喻统摄的叙事型长页）
- **新子技能** `subskills/design-metaphor-narrative/SKILL.md`（十节 + 自检清单）：单一隐喻统摄信息架构（映射覆盖率 ≥80% 判据、去隐喻测试）、门禁序章（可跳过 / 一次性 / 可回退 / 失败放行四铁律）、数据字段的实物替身（语义场一致为硬要求）、叠放式缩略预览（旋转与偏移参数区间）、极窄色板 + 单点语义化强调色、三字体系统（sans 叙述 / mono 读数 / display 数字）、长页 reveal 错峰（每项 +60ms）、固定底栏渐进模糊（5 层递进 backdrop-filter + mask 限定作用带）、长文 gutter 章节标签、收尾落在"人"上、重型效果前置能力检测。
- **分散并入通用层**（遵循「子技能承载细节、modules 只留一行引用」原则）：
  - `04-信息叙事/故事逻辑.md`「补充」：隐喻统摄 + 门禁序章 + 收尾落人。
  - `03-形式组织/关系.md`「补充」：字段实物替身 + 叠放缩略预览。
  - `02-感知物料/动画.md`「补充」：reveal 错峰 + 底栏渐进模糊 + 能力检测（通用红线，凡引入 WebGL/大视频/重动画的开场均适用）。
  - `02-感知物料/颜色.md`「补充」：极窄色板 + 单点语义化强调色（与「色彩作结构·加色构成法」对偶：一个管单页构成，一个管全站纪律）。
  - `02-感知物料/文字.md`「补充」：三字体系统（分工即语义）。
- **路由与索引**：SKILL.md frontmatter description 补「隐喻统摄叙事长页」；交叉资产新增一条；子技能路由表新增 4 行；目录结构新增该子技能；来源署名计数 8 → 9 份。
- **来源署名**：新增 `references/来源署名/MikeBarton-mikes-cv.md`。该源为**无公开授权的成品作品**（全页 noindex、无 source map、无仓库），与目录内其余开源/公开来源性质不同，边界按「硬红线 / 自由层 / 软红线近似度自检」三层处理（当日已按用户反馈校准，见下条修订）。README 索引加警示块。
- version 1.2.0 → 1.3.0。

### 1.3.0 修订（同日，按用户反馈校准合规口径）
- **撤回过严条款**：初版在署名文件中写了「禁复用其隐喻本身」，该条**一刀切过严，已撤回**。理由：旅行 / 航程 / 登机属设计界通用母题（登机牌请柬、boarding pass 模板早已泛滥），是著作权不保护的「思想」层；受保护的是具体画出来的**执行物**。
- **红线重构为三层**：硬红线（禁美术资产 / 字体文件 / 原文案 / 代码）→ 自由层（通用母题与通用技法不设限）→ 软红线「整体近似度自检」（5 条判据命中 ≥3 条即告警，须换掉 ≥2 个元素的**具体执行**拉开距离，而非推倒重来）。
- 同步修订：`subskills/design-metaphor-narrative` 头部合规口径、自检清单（拆成「合规」+「近似度自检」两条）、备注（新增「母题不设限，执行要分离」与「隐喻质量看映射不看新颖」）；SKILL.md 交叉资产条；署名 README 警示块。
- **防漏兜底**：`modules/01-场景.md` 小节 5 新增「内容形态的二次路由」——第 1 层识别到经历/时间节点/履历/产品故事类内容时，立即路由 `design-metaphor-narrative`，不等到第 4 层。（回应子技能"不常驻可能漏触发"的固有缺陷。）

## [1.2.0] - 2026-08-28

### 审美专项（由审美诊断驱动：防丑机器 → 造美引擎）
- **审美意图卡**（`modules/01-场景.md` 新增小节 5）：第 1 层收口后、动手前必填——3 主感觉词 + 1 视觉对标 + 1 破格点；把第五层「感觉」从末端验收提前为创作方向盘（模式 1/2/3 各自接法）。
- **红点笔记协议**（`design-reference-sourcing`）：抓回参考必写 3 行（好在哪 / 机制 / 可复用），沉淀进 taste-profiles `notes` 字段与 `references/惊艳手法库.md`，替代原「多为 TODO」的登记方式。
- **惊艳手法库**（新增 `references/惊艳手法库.md`）：7 个进攻性造美手法（撞色穿灰场 / 出血破网格 / 极简藏精雕 / 违反自定规则 / 文字错位叠压 / 极端缩放 / 呼吸位），与「反模板校准」对偶；纪律：一轮最多 1 个、落点即破格点。
- **克制降级为默认值 + 破格触发器**（`动画.md` 与 `design-frontend`）：「同屏 ≤3–5 动效」由硬上限改为默认预算；单焦点 / signature 记忆点元素允许 1 个破格（其余让位错峰）。
- **审美练习回路**（新增 `审美练习回路.html`）：每日 10 分钟看→品→做→记工具，12 个任务池 + 红点笔记记录 + localStorage 打卡。
- version 1.1.0 → 1.2.0。

### 1.2.0 灌水（视觉记忆库首批）
- 用 `agent-browser` 抓取 4 张真实参考图：`athens-week-fullpage`（Wild Week–Athens 长页）、`editorial-ref-01`（It's Nice That）、`dark-tech-ref-01`（Linear）、`warm-artisan-ref-01`（Cosmos 调性板）。
- 4 个 taste-profile 首次填满 `notes.red_dot`（好在哪 / 机制 / 可复用 + caveat）；`references` TODO 全清，指向真实文件。
- `references/惊艳手法库.md` 新增手法 8「用字号代替装饰」——由 editorial 红点笔记的「可复用」条目提炼，兑现"参考 → 红点 → 手法库"管道。
- 来源合规：仅作内部参考 / 灵感，不擅自再发布第三方资产（沿用 design-reference-sourcing 红线）。

## [1.1.0] - 2026-08-28

### 优化（由 skill-optimizer 驱动，run: design-workflow-2026-08-28T044730Z）
- **一致性修复**：父 SKILL.md 中「联网找素材」策展源计数由 6 个更正为 9 个（补 ThreeUI / React Bits / Spline Community），并以子技能 `design-reference-sourcing` 清单为准；目录结构注释同步。
- **去除陈旧占位**：模式 2 吸睛「（暂为占位，按接入点引用『独特品味 skill』）」更正为已内建的 `design-taste` 子技能引用；模式表格「待设计」占位改为「—」。
- **署名计数精确化**：来源署名目录由「9 份」更正为「8 份来源 + README 索引」。
- **子技能维护**：`design-taste` 目录树补 athens-week 实例；`design-reference-sourcing` 删除过时的「（需新建）」目录注释。
- **可维护性**：新增本 CHANGELOG；version 由 1.0.0 提升至 1.1.0（此前 version 号低估了技能成熟度）。

### 已知剩余 / 后续建议（未在本轮自动修改）
- `design-ip-assets` 作为通用技能深度内嵌了单一具体 IP（三果团）作为贯穿示例；建议将具体实例抽离为 `assets/` 下的示例资产或明确标注「以下为示例」，与通用 schema 分离，提升复用性。
- `design-taste` / `design-reference-sourcing` 各 profile 的 `references` 参考图（.png）尚未补入，属待补资产缺口。
- `design-reference-sourcing` 中含本机绝对路径示例，跨环境移植时需注意。

## [1.0.0] - 基线
- 五层顺序设计工作流（场景 → 感知物料 → 形式组织 → 信息叙事 → 主观输出）建立。
- 三种调用模式（领域适配 / 吸睛 / AI 自主）。
- 8 个专项子技能（chart / typographic-glyph / icon-batch / photo-sketch / frontend / taste / ip-assets / reference-sourcing）抽离完成。
- 来源署名按源分文件管理（8 份来源 + README 索引）。
